# JANUS Vision - Execution Monitoring Alert Rules
#
# All rules removed 2026-07-01: every alert in this file targeted the Vision
# execution service (vision_execution_* metrics / job=~"vision-.*"), which is
# not deployed in this stack — zero vision_* series are exported and no
# vision-* scrape target exists, so none of the 22 rules could ever fire
# (including VisionServiceDown, whose job selector matched no target).
# Restore from git history when the Vision execution service ships.

groups: []
