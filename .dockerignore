# .dockerignore — Exclude unnecessary files from Docker build context
# Speeds up builds and keeps images small.

# ============================================================================
# VERSION CONTROL
# ============================================================================
.git
.gitignore
.gitattributes
.github
.gitlab-ci.yml

# ============================================================================
# ENVIRONMENT & SECRETS
# ============================================================================
.env
.env.*
!.env.example

# ============================================================================
# DOCUMENTATION
# ============================================================================
# Exclude standalone markdown files at the repo root (changelogs, license text,
# etc.) but keep README.md for context. The docs/ directory is NOT excluded here
# because prod image builds may want to COPY it in; in docker-compose dev it is
# bind-mounted (./docs:/app/docs) for hot-reload without an image rebuild.
*.md
!README.md
LICENSE
notebooks/
book/
target/doc/

# ============================================================================
# EDITORS & IDEs
# ============================================================================
.vscode/
.idea/
.idea_modules/
.vs/
*.swp
*.swo
*.sublime-*
*~

# ============================================================================
# RUST
# ============================================================================
**/target/
**/*.rs.bk
**/*.rmeta
*.pdb

# sqlx offline query cache — generated by `cargo sqlx prepare`, not needed
# in the Docker build context (the codebase uses dynamic query() calls only,
# so SQLX_OFFLINE=true works without any cache files present).
.sqlx/

# ============================================================================
# KOTLIN / KMP
# ============================================================================
**/build/
**/.gradle/
*.iml
local.properties

# ============================================================================
# PYTHON
# ============================================================================
.venv
__pycache__
*.pyc
*.pyo
*.egg-info
dist/

# ============================================================================
# NODE.JS
# ============================================================================
node_modules/
npm-debug.log*
yarn-error.log*

# ============================================================================
# TESTING
# —— NOTE: only the top-level src/tests/ (Python) is excluded here.
# —— src/ruby/tests/ is intentionally kept in the build context.
# ============================================================================
src/tests/
test-results/
TestResults/
coverage/

# ============================================================================
# DOCKER (avoid recursive / stale context)
# ============================================================================
docker-compose*.yml
Dockerfile*
!Dockerfile
!docker/Dockerfile
!docker/Dockerfile.gpu
.dockerignore

# ============================================================================
# SCRIPTS (host-only, except the monitoring bridge Docker needs)
# ============================================================================
scripts/*
!scripts/monitoring/alertmanager-discord.py
!scripts/monitoring/requirements-bridge.txt
run.sh
run.train.sh
Makefile

# ============================================================================
# MODELS (bind-mounted at runtime, never baked into an image)
# ============================================================================
models/

# ============================================================================
# DATA, LOGS & DATABASES
# ============================================================================
*.csv
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3
*.log
data/
dataset/
projects/
outputs/
logs/

# ============================================================================
# BUILD ARTIFACTS & ARCHIVES
# ============================================================================
*.zip
*.tar.gz
*.rar

# ============================================================================
# CACHES
# ============================================================================
.ruff_cache/
.pytest_cache/
.mypy_cache/
**/.mypy_cache/

# ============================================================================
# TEMPORARY & OS FILES
# ============================================================================
tmp/
temp/
*.tmp
*.bak
*.backup
*.orig
.DS_Store
Thumbs.db

# ============================================================================
# AI TOOLS
# ============================================================================
.claude

# ============================================================================
# PROJECT-SPECIFIC
# ============================================================================

ort_cache/

sql/
nginx/
.local
trace/
debug/
file_contents.txt
codebase_analysis_report*/
perf.json
